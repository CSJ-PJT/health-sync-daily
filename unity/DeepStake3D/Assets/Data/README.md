# DeepStake3D Data

This is the active production-side data folder for Deep Stake.

Use it for:
- world prototype JSON
- NPC and quest datasets
- settlement palette data
- commerce/unlock mappings
- faction/alignment datasets

`DeepStakeUnity` data files are reference-only unless explicitly ported here.
