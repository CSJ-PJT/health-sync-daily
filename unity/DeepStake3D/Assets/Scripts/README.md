# DeepStake3D Scripts

`DeepStake3D` is the only active Unity production client.

`DeepStakeUnity` is legacy/reference only.

## Rules

- quarter-view 3D is the active renderer direction
- local/offline mode must work first
- raw health records must never enter the Unity client
- only derived health-link values may cross from the health app
